model = "gpt-5-codex"
model_reasoning_effort = "high"

sandbox_mode = "workspace-write"
approval_policy = "on-request"

project_doc_fallback_filenames = ["TEAM_GUIDE.md", ".agents.md", "ARCHITECTURE.md"]
project_doc_max_bytes = 65536

[agents]
max_threads = 4
max_depth = 1

[profiles.default]
model = "gpt-5-codex"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
approval_policy = "on-request"

[profiles.review]
model = "gpt-5-codex"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
approval_policy = "never"

[profiles.audit]
model = "gpt-5-codex"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
approval_policy = "never"